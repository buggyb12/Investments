[phases.setup]
nixPkgs = ["nodejs_20", "npm-10_x"]

[phases.install]
cmds = [
  "rm -rf node_modules/.cache || true",
  "npm install --no-audit --no-fund --prefer-offline"
]

[phases.build]
cmds = ["npm run build"]

[start]
cmd = "npm start"
